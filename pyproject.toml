[project]
name = "restaurant-phone-agent"
version = "0.1.0"
description = "AI voice agent for answering restaurant customer calls."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "websockets>=12.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
dev-dependencies = []
